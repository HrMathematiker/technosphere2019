#include <iostream>
#include <vector>

int main() {
    int N, input, min = 0;
    std::vector<int> coins;
    for (int i = 0; i < N; i++) {
        std::cin >> input;
        coins.push_back(input);
    }
    for (int i = coins.begin(); i <= coins.end(); i++) {
        
    }
    
    
}