#include <iostream>
#include <vector>

int main() {
    int j, N, input;
    int64_t sum = 0;
	uint64_t sum = 0;
	std::vector<int> seq_1, seq_2;
	std::cin >> N;
    for (int i = 0; i < N; i++) {
        std::cin >> input;
        seq_1.push_back(input);
    }
    j = 0;
    while (j != 2 * N) {
        std::cin >> input;
        if (input )
    }
